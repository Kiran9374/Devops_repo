Hi, this is my first file
my second code is ready 
Hi hello,
git --version
git which
git add.
git commit singapur -m "new commit"
git push origin master
git tag -a "important" -m "new tag" <commit ID>
